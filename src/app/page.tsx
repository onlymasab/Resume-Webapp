import React from 'react';
import FormContainer from './Logic/page';

const Home: React.FC = () => (
  <div className="min-h-screen bg-gray-100 flex items-center justify-center">
    <FormContainer />
  </div>
);

export default Home;
